-define(MACHINE_LISTEN_PORT, 4343).
-define(SUNDAY_SERVER_PORT, 4242).
-define(FINGER_SERVER_PORT, 79).

-define (LOG_DB_SERVER, "db.csh.rit.edu").
-define (LOG_DB_USERNAME, "drink").
-define (LOG_DB_DATABASE, "drink").